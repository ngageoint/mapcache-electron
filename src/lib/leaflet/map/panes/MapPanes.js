const GRID_SELECTION_PANE = {
  name: 'gridSelectionPane',
  zIndex: 625
}

const BASE_MAP_PANE = {
  name: 'baseMapPane',
  zIndex: 200
}

const EDITING_PANE = {
  name: 'editingPane',
  zIndex: 501
}

const SEARCH_RESULT_POINTS_ONLY_PANE = {
  name: 'searchResultPointsPane',
  zIndex: 601
}

const SEARCH_RESULTS_PANE = {
  name: 'searchResultsPane',
  zIndex: 510
}

const DRAWING_MAP_PANE = {
  name: 'drawingMapPane',
  zIndex: 501
}

export {
  GRID_SELECTION_PANE,
  BASE_MAP_PANE,
  EDITING_PANE,
  SEARCH_RESULTS_PANE,
  SEARCH_RESULT_POINTS_ONLY_PANE,
  DRAWING_MAP_PANE
}
