const BASE_MAP_PANE = {
  name: 'baseMapPane',
  zIndex: 200
}

const OVERLAY_PANE_FEATURES = {
  name: 'overlayPaneFeatures',
  zIndex: 400
}

const SEARCH_RESULTS_PANE = {
  name: 'searchResultsPane',
  zIndex: 500
}

const SEARCH_RESULT_POINTS_ONLY_PANE = {
  name: 'searchResultPointsPane',
  zIndex: 550
}

const DRAWING_LAYER_PANE = {
  name: 'drawingLayerPane',
  zIndex: 600
}

const DRAWING_VERTEX_PANE = {
  name: 'drawingVertexPane',
  zIndex: 651
}

const GRID_SELECTION_PANE = {
  name: 'gridSelectionPane',
  zIndex: 800
}

export {
  GRID_SELECTION_PANE,
  BASE_MAP_PANE,
  SEARCH_RESULTS_PANE,
  SEARCH_RESULT_POINTS_ONLY_PANE,
  DRAWING_LAYER_PANE,
  DRAWING_VERTEX_PANE,
  OVERLAY_PANE_FEATURES
}
